import React from "react";

export default (props) => {
  return (
    <div>
      <h1>Home</h1>
      <div>
        feature 1: when the page loads, it should return all topics in the
        forum. - done
      </div>
      <div>
        feature 2: Upon clicking on the view comments, it should only show 1
        topics and it's comments. - done
      </div>
      <div>
        feature 3: all users can add their comments to the topic - done (need
        refactor) do testing
      </div>
      <div>
        feature 4: all users can add topics for users to comment - not started
      </div>
    </div>
  );
};
